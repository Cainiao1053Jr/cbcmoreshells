对于dual cannon 的炮弹，我需要根据DualCannonBehavior.Kind定义的类型对击穿模型进行划分
当前的击穿模型直接调用DualCannonPenetrationModel.resolve()
我需要一个功能，根据炮弹的类型对这个函数内做细小修改，当类型为HSAP时，toughness的计算换用别的方式
新的toughness采用方块累积，设定初始累积系数accCoef为1，朝着炮弹击穿的内侧的法向量进行累积，每步进一个方块，系数降低一个固定值，在类前面用一个变量调控，设置为TOUGHNESS_REDUCTION_RATE
初始设定为0.25，比如到第二个方块时，累积这个方块0.75的硬度，采用累积值作为硬度，当累积系数小于0时退出并结算，同时，如果碰到方块为空气（用小于一个固定值判定，可以给1，也用变量调控，就不去getBlockState了）则直接结算
对于这一分支的后续逻辑，momentum在计算时不考虑incidence，直接用cappedMomentum是否大于toughness进行判定，在总体的逻辑中，后续部分会把这类炮弹的质量在击穿后清空
其余逻辑不变，DualCannonPenetrationModel和DualCannonBehavior中修改并适配